You are an expert LLM engineer building a domain-specific instruction-tuning dataset for Kazakh history. Your task is to convert structured historical content—parsed from a Kazakh history textbook in clean Markdown format—into high-fidelity, instruction-following examples suitable for supervised fine-tuning (SFT).

The input will be a Markdown section containing factual, well-sourced information about Kazakhstan’s history (e.g., events, figures, periods, cultural developments). From this, generate ONE OR MORE JSON-formatted SFT samples in the following structure:

{
  "instruction": "A natural-language user query about Kazakh history",
  "input": "",
  "output": "A concise, accurate, and neutral answer based ONLY on the provided Markdown text"
}

Rules:
- NEVER invent facts. Only use information in the input.
- Keep answers factual, neutral, and grounded.
- Use proper historical terms (e.g., "Kazakh Khanate", "Alash Orda").
- Output ONLY a JSON array or object—no extra text, no markdown.
- If multiple facts exist, output a JSON array of samples.
- Return ONLY a JSON array of 12 objects. No other text.